class NoteSize:
    def __init__(self, whiteWidth, whiteHeight, blackWidth, blackHeight):
        self.whiteWidth = whiteWidth
        self.whiteHeight = whiteHeight
        self.blackWidth = blackWidth
        self.blackHeight = blackHeight
